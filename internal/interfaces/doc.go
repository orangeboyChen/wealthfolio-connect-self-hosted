// Package interfaces contains delivery-mechanism adapters: HTTP handlers,
// middleware and request/response models. Handlers translate HTTP requests
// into application service calls; they MUST NOT contain business logic.
package interfaces
