// Package application contains application services (use cases) that
// orchestrate domain entities, repositories and external ports.
// It depends only on interfaces declared in the domain layer.
package application
